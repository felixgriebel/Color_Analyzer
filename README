This is the second version of the Color-Analyser.

Either download the sourcecode or download the .exe-file

There may be problems with executing the application, because the windows-defender doesn't accept it, 
just (press "further information" on windows and) allow it to execute
